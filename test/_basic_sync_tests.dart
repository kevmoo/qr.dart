part of console_test_harness;

class SyncTests {
  static void run() {
    test('true result is cool', pending);
    test('false result fails', pending);
    test('null result is sad', pending);
  }
}